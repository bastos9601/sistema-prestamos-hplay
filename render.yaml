services:
  # Aplicación web Flask (sin base de datos externa por ahora)
  - type: web
    name: prestamos-web
    env: python
    plan: free
    buildCommand: pip install -r requirements-ultra-minimal.txt
    startCommand: gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.8.16
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: MAIL_SERVER
        value: smtp.gmail.com
      - key: MAIL_PORT
        value: 587
      - key: MAIL_USE_TLS
        value: true
      - key: MAIL_USERNAME
        value: bastosbarbaranvictor@gmail.com
      - key: MAIL_PASSWORD
        value: ldkhglaolpcofqbs
      - key: LOG_LEVEL
        value: INFO
